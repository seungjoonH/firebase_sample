import 'package:flutter/material.dart';

class CloudFirestorePage extends StatelessWidget {
  const CloudFirestorePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
